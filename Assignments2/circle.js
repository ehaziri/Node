
var circle = require('calculate_properties'),
    radius = 5;

console.log(`The area of a circle with radius`+
` ${radius} is ${circle.area(radius)} and `+
` the circumference is ${circle.circumference(radius)}`);
